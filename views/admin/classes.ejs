<%- include('../partials/header', { title: 'Classes' }) %>

<h3 class="mb-4">Manage Classes</h3>

<div class="card p-4 mb-4">
  <form method="POST" action="/admin/classes" class="row g-2">
    <div class="col-md-8">
      <input type="text" name="name" class="form-control" placeholder="e.g. JSS1A, Primary 5, SS2 Science" required>
    </div>
    <div class="col-md-4">
      <button type="submit" class="btn btn-primary w-100">Add Class</button>
    </div>
  </form>
</div>

<div class="card p-4">
  <table class="table">
    <thead><tr><th>Class Name</th><th></th></tr></thead>
    <tbody>
      <% classes.forEach(function(c) { %>
        <tr>
          <td><%= c.name %></td>
          <td class="text-end">
            <form method="POST" action="/admin/classes/<%= c.id %>/delete" onsubmit="return confirm('Remove this class?');">
              <button class="btn btn-sm btn-outline-danger">Delete</button>
            </form>
          </td>
        </tr>
      <% }) %>
      <% if (!classes.length) { %>
        <tr><td colspan="2" class="text-muted text-center">No classes added yet.</td></tr>
      <% } %>
    </tbody>
  </table>
</div>

<%- include('../partials/footer') %>
